spring:
  sql:
    init:
      mode: never
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        ddl-auto: create
        use_sql_comments: true
  flyway:
    enabled: false
logging:
  level:
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

