tasks.named('bootJar') {
    enabled = false
}

tasks.named('jar') {
    enabled = true
}

dependencies {
    implementation project(':table')
    implementation project(':order')
    testImplementation project(':test-app')
}
