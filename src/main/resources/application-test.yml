spring:
  h2:
    console:
      enabled: false
  jpa:
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

logging:
  level:
    org.hibernate.type.descriptor.sql.BasicBinder: trace
