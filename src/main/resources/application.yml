logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: TRACE
spring:
  jpa:
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    hibernate:
      ddl-auto: validate
  h2:
    console:
      enabled: true

---

spring:
  jpa:
    hibernate:
      ddl-auto: create
