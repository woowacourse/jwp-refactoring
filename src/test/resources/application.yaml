logging:
  level:
    org:
      springframework.jdbc.core.JdbcTemplate: TRACE
      hibernate.type.descriptor.sql.BasicBinder: TRACE

spring:
  flyway:
    enabled: false
  jpa:
    properties.hibernate.format_sql: true
    show-sql: true
  h2:
    console:
      enabled: true
      settings:
        trace: true
