spring:
  h2:
    console:
      enabled: true
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb;MODE=MySQL
  flyway:
    enabled: false
logging:
  level:
    org:
      springframework:
        jdbc: debug
