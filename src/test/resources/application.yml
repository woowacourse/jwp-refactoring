spring:
  profiles:
    active: test
---
spring:
  profiles: test
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb
  flyway:
    enabled: false
  jpa:
    properties:
      hibernate:
        format_sql: true
    show-sql: true
logging:
  level:
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE