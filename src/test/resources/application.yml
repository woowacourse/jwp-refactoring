spring:
  flyway:
    enabled: false

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  datasource:
    initialization-mode: always
    schema: classpath:db/migration/V1__Initialize_project_tables.sql