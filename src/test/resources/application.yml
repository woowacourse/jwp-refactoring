logging:
  level:
    org:
      hibernate:type:descriptor:sql:BasicBinder: TRACE
spring:
  jpa:
    properties:
      hibernate:format_sql: true
    show-sql: true
  h2:
    console:
      enabled: true
