spring:
  jpa.properties.hibernate:
    show_sql: true
    format_sql: true
    use_sql_comments: true
  flyway:
    enabled: false

logging:
  level:
    org:
      springframework:
        jdbc:
          datasource:
            init:
              ScriptUtils: debug
