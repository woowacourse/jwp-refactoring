bootJar { enabled = false }
jar { enabled = true }

dependencies {
    implementation(
            project(':module-menu'),
            project(':module-ordertable'),
            project(':module-common')
    )
}
